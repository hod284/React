version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitoring-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - monitoring-network

networks:
  monitoring-network:
    driver: bridge
